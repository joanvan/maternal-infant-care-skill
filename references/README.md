# References Layout

- `zh/` — Chinese references (full set, expanded operational content)
- `en/` — English references (core operational set; expand as needed)

If user language is Chinese, prefer `zh/`.
If user language is English, prefer `en/` and adapt from `zh/` when needed.
